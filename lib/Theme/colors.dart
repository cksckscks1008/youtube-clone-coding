import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color background = Color(0xFF0F0F0F);
  static const Color bottomNavBackground = Colors.black;

  static const Color surface = Colors.white12;
  static const Color surfaceSelected = Colors.white;

  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white60;
  static const Color textDisabled = Colors.white38;

  static const Color grey = Colors.grey;
  static const Color primary = Colors.blue;
}